#!/usr/bin/env ruby
require_relative "../lib/turn.rb"

board = Array.new(9, " ")
puts "choose movement 'type between 1-9'"
input = gets.strip
position = input_to_index(input - 1)
valid_move?(board, position)
move(board, position, "X")
display_board(board)